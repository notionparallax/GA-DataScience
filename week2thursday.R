#week 2 - Thursday
#KNN supervised clasification

#train on about 70% of the data and test it on the rest
#if training data is performing well, but breaks on test data: indiacates overfitting.

#data analysis with OS tools - Philipp k janert (overfitting diagram)
# dtreg.com underfittign and overfitting examples

install.packages(c(class, psych, ggplot2, GGally)) #this needs needs to be c()'d into a vector

library(class)
library(psych)
library(ggplot2)
library(GGally)
